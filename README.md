A rocket simulator that simulates the flight of rockets!

The up and down arrow keys control the angle of attack of the rocket. Left `Ctrl` decreases the throttle while left `Shift` increases the throttle.